import React from 'react'
import PropTypes from 'prop-types'

const TaskItem = props => {
    return (
        <div>
            
        </div>
    )
}

TaskItem.propTypes = {
    TaskItem: PropTypes.array.isRequired,
}

export default TaskItem
